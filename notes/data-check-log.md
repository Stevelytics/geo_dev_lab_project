# Data Check Log (optional, not graded — but saves you in month six)

Use this as scratch space while you work through "check each one exists."
Log every dataset you *tried*, not just the ones that made it into the final brief.
When one fails, write down why — it's the fastest way to notice a question needs to change.

| Dataset tried | Source checked | Result | Notes |
|---------------|-----------------|--------|-------|
| e.g. flood-risk zones | State ministry portal | ❌ Not published | Agency requires a data request letter, 6+ week turnaround |
| e.g. river network | OpenStreetMap (Geofabrik extract) | ✅ Downloaded | ~4MB, covers full LGA, waterway tag present |

---

**This week's target (download hardest first):** GHS-BUILT-S multi-temporal layer from GHSL, clipped to Ibeju-Lekki LGA. It's global and free, but comes as large raster tiles that need reprojecting and clipping to a small area you don't yet have a clean boundary polygon for — get the boundary (row above it in the brief) and this layer onto your machine this week, before writing anything else.
